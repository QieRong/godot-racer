# ── Godot 4 引擎产物 ─────────────────────────────────────
# 导入缓存与着色器缓存：体积大、可自动重建，不入库
.godot/

# ── 运行日志 ─────────────────────────────────────────────
godot-logs/
*.log

# ── 操作系统 / 编辑器杂物 ────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# 截图产物（截图钩子 --shot 的输出）：godot-shot*.png 是刻意保留的画面记录，入库
# 这里只排除临时中间产物

# ── 密钥 / 本地配置（必须排除，绝不能让 API key 进仓库）──────
# 模板 openrouter.cfg.example 是要入库的；用户复制出来的这份不进
openrouter.local.cfg
openrouter.cfg
*.local.cfg
.env
.env.*

