# ── Godot 4 引擎产物 ─────────────────────────────────────
# 导入缓存与着色器缓存：体积大、可自动重建，不入库
.godot/

# ── 运行日志 ─────────────────────────────────────────────
godot-logs/
*.log

# ── 操作系统 / 编辑器杂物 ────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# 截图产物：刻意保留的画面记录统一放在 screenshots/（已入库）。
# --shot 钩子默认输出到 godot-logs/（已忽略），临时产物不入库。

# ── 密钥 / 本地配置（必须排除，绝不能让 API key 进仓库）──────
# 模板 openrouter.cfg.example 是要入库的；用户复制出来的这份不进
openrouter.local.cfg
openrouter.cfg
*.local.cfg
.env
.env.*

# 临时文件（例如写提交信息用的 .tmp）：绝不入库
*.tmp

# --shot 钩子的默认输出（临时产物；正式画面记录放 screenshots/）
godot-shot*.png
