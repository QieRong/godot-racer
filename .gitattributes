# Windows 批处理必须用 CRLF 换行：cmd 按 CRLF 切行，只有 LF 会把多行黏成
# 一条命令，报「不是内部或外部命令」「文件名、目录名或卷标语法不正确」。
# 实测：LF 版 exit=1，只把换行改成 CRLF 后 exit=0。
#
# 注意：这条规则只管「检出时」的换行。用工具/脚本直接写 .bat 时，仍要自己
# 写 CRLF，否则本地那份又会被写坏（踩过一次）。
*.bat text eol=crlf
*.cmd text eol=crlf
